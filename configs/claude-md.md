# General

## CLI Notes

- Keep machine-specific and work-specific instructions in a local, untracked file.
- Keep secrets in `~/.env.local`; do not commit tokens, account IDs, internal URLs, or private hostnames.
